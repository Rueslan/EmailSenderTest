<Window x:Class="EmailSender.Views.SenderEmailListEditView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:EmailSender.Views"
        xmlns:vm="clr-namespace:EmailSender.ViewModels"
        xmlns:xc="clr-namespace:EmailSender.Services"
        mc:Ignorable="d"
        xc:DialogCloser.DialogResult="{Binding DialogResult}"
        Title="{Binding Title}" Height="120" Width="220" ResizeMode="NoResize" WindowStartupLocation="CenterScreen">
    <Window.DataContext>
        <vm:SenderEmailListEditViewModel/>
    </Window.DataContext>
    <Grid>
        <TextBox x:Name="tbEmail" HorizontalAlignment="Left" Height="23" Margin="46,10,0,0" TextWrapping="Wrap" VerticalAlignment="Top" Width="152" Text="{Binding tbEmail}"/>
        <TextBlock HorizontalAlignment="Left" Margin="10,11,0,0" TextWrapping="Wrap" Text="Email:" VerticalAlignment="Top"/>
        <Button x:Name="btnAccept" HorizontalAlignment="Left" Margin="10,54,0,0" VerticalAlignment="Top" Width="75" IsDefault="True" Content="{Binding btnAcceptContext}" Command="{Binding btnAcceptCommand}"/>
        <Button Content="Отмена" HorizontalAlignment="Left" Margin="123,54,0,0" VerticalAlignment="Top" Width="75" IsCancel="True"/>

    </Grid>
</Window>
